import { script } from '../../../helpers/script';
import { module, test } from 'qunit';

module('Unit | Helper | script');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = script(42);
  assert.ok(result);
});
